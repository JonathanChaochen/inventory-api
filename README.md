# Inventory App Node API

### `Install`

Run

```sh
npm install
```

### `Start`

Before start running server on localhost. You need download and install MongoDB first.

Enter command mongod after that.

Run

```sh
npm start
```

### `Test`

Run

```sh
npm test
```

The server will run on localhost port 5000
